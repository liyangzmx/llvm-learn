# 文档验证记录

基准：本地 LLVM `release/18.x` / `3b5b5c1ec4a3095ab096dd780e84d7ab81f3d7ff`。

## 初次静态验证已完成

- official 第 1～7 章、aiversion 第 0～7 章及两套目录齐全。
- 7 个扩充章节通过对应 official 正文的前缀包含检查；标题数量/字符下限仅防止明显截断，不是扩充质量或英文覆盖率证明。
- C++/TableGen/MLIR 代码块共 140 处：59 处逐字源码、8 处译编、73 处示意、未分类 0 处。
- 逐行核对只覆盖带源码锚点的 59 处逐字摘录（去重为 36 段）；其他 81 处不是逐字源码，不在逐行比对范围内；后续运行复核另覆盖了其中的完整 MLIR 示例，见文末。
- 186 个来源文件的 SHA-256 与记录基准一致。
- 所有文档中的本地 Markdown 链接及明确引用的 Toy 测试文件存在。
- 代码围栏闭合，60 组 shell 代码块通过 bash -n 语法检查，其中教材 59 组、仓库首页 1 组。
- 13 个 Mermaid 块具备 flowchart 声明及关系边；尚未做渲染验收。
- LLVM 许可证副本的文本与本地原件一致（忽略文件尾空白）。
- 校验器新增标签强制检查；内存变异回归测试确认它会拒绝缺少标签、逐字源码缺少锚点、逐字摘录被修改的情况，测试未改写任何教材或上游文件。
- 本任务未修改本地 LLVM 教程、示例、测试及现有 build。

## 复查

在仓库根目录执行：

```bash
node mlir/toy/scripts/validate-materials.mjs
node mlir/toy/scripts/validate-materials.test.mjs
```

主检查通过时输出 PASS 与各项计数；回归测试输出正常基线及三个拒绝场景的结果。失败时列出具体文件和原因。脚本只读检查，不执行文档中的构建命令，也不会自动更新源码指纹。

历史 ds4.1 与 GLM 复核记录保存在 Git 历史中，已移出当前文档树。2026-09-13 的 GLM 修订后主校验及内存变异回归测试均通过；该轮没有改变代码块来源分类数量。

## 本轮代码与实验细化

新增 13 段源码摘录与 25 组实验命令，覆盖第 0～7 章；official 正文没有修改。新实验的导航、源码引用和两个独立输入文件的链接已检查，全文包含关系与原回归测试继续通过。

新增的 shared-transpose 与 live-reshape 输入只做了源码驱动的人工推导，没有用 toyc 运行；后者有意展示当前 lowering 的不支持边界。此处“输入存在”和“源码摘录一致”不代表示例已编译成功或失败诊断已经实测。

## 2026-09-13 知识库目录整理复查

Toy 教材整体迁入 `mlir/toy/`，仓库根目录增加 LLVM / MLIR 导航。已修正旧绝对路径、校验命令和已删除复核文件的引用；实验通过 `TOY_ROOT` 定位教材，构建产物仍放在仓库根目录的 `build-llvm18/`，并加入忽略规则。

主校验与三个内存变异回归测试通过：21 份文档、203 处本地链接、59 处源码摘录、186 个来源文件指纹、7 组章节包含关系。另行核对 8 处章节锚点和 4 处 `TOY_ROOT` 输入引用，并从仓库根目录、教材目录、扩充教材目录分别验证输入定位。此轮未执行 LLVM 构建或 Toy 程序。

## 2026-09-13 使用已有构建的运行复核

已确认现有 `/opt/llvm-project/build` 启用 MLIR、Examples 和 Native，七章 toyc 均为 LLVM 18.1.8。逐章执行了 43 组非构建实验命令、12 个完整 Toy/MLIR 片段、56 个官方测试（60 条 RUN），并额外核对 JIT 精确输出和教材描述的实现边界。官方测试全部通过；完整片段中的不合法 Toy 操作按未注册方言的宽松解析规则单独解释。

本轮未重新配置或构建 LLVM，也未修改上游源码和测试。已将默认 TOY_BUILD 改为现有构建，各章从自动构建改为检查二进制；详细结果、lit 的临时 JIT 探测适配和原始证据见 [运行验证报告](aiversion/RUNTIME-VALIDATION.md)。

## 尚未覆盖

未将 C++/TableGen 讲解片段拼装为独立项目编译；其逐字摘录已与实际 LLVM 18 源码核对，TableGen 的四种生成入口及 DRR 生成入口已实际执行。没有对所有异常输入作穷举验证。

未安装 Markdown/Mermaid 渲染依赖，没有将静态围栏检查冒充图形布局验收。读者使用支持 Mermaid 的 Markdown 阅读器即可查看图示。

静态检查与实测各有边界；只有运行报告列出的项目被计为已运行，不能据此保证所有源语言输入都得到正确诊断。环境与实验步骤见 [第 0 章](aiversion/00-preflight.md)。
